import { categoryType } from "./category";
import { articleType } from "./article";
import { videoType } from "./video";
import { eventType } from "./event";

export const schema = {
  types: [categoryType, articleType, videoType, eventType],
};
